#!/usr/bin/env bash
# Script: fix_nginx_port.sh

# Install required packages
sudo apt-get update
sudo apt-get install -y nginx

# Modify Nginx configuration to listen on port 80
sudo sed -i 's/^\s*listen\s*\(.*\)/listen 80;/g' /etc/nginx/nginx.conf

# Restart Nginx
sudo service nginx restart

